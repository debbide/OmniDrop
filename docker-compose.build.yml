# Build the single OmniDrop image from local source
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build

services:
  omnidrop:
    image: omnidrop:local
    build:
      context: .
      dockerfile: docker/Dockerfile
